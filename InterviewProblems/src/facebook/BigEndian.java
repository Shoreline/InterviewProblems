package facebook;
/**
 * Shall be a question for C++ only.
 *
 */
public class BigEndian {
/*
    bool IsBigEndian()
    {
        int a =1 ; 
        if(((char*)&a)[3] ==1) // 由于a是int，所以占四个字节，其值是00000001，存储方式如下。所以a[3]是0，不是大端序。一个更标准的写法是将a[3]替换为a[sizeof(int) - 1]
            return true ;
        else
            return false ;
    }
*/
}
